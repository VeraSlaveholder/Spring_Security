version: "3.9"
services:
  postgres:
    image: postgres:latest
    container_name: container
    environment:
      POSTGRES_DB: "hibernate_demo_db"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "AliasVera2003"
    ports:
      - "5432:5432"
